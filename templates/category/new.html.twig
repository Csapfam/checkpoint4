{% extends "base.html.twig" %}

{% block body %}
    <h4 class="new-add">Ajout d'une nouvelle catégorie</h4>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    <div class="form">
    {{ form_widget(form) }}
    <input type='submit' value='Ajouter' class="btn-add">
    </div>
    {{ form_end(form) }}
{% endblock %}